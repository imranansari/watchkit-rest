source 'https://github.com/CocoaPods/Specs.git'
use_frameworks!

target 'RestButton' do
  platform :ios, '10.0'
  pod 'Alamofire', '~> 4.0'

end

target 'RestButton WatchKit App' do
  platform :watchos, '3.0'
  pod 'Alamofire', '~> 4.0'

end

target 'RestButton WatchKit Extension' do
  platform :watchos, '3.0'
  pod 'Alamofire', '~> 4.0'

end
